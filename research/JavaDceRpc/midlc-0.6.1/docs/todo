Implement {enc,dec}_ndr_* routines and try to compile stubs.

Include statements in the C stubs are incorrect if -o is used to place output files in a different directory. The output directory needs to be used rather than the source directory.

Complete server stub emitter.

Implement preprocw.c in the win32 env

Complete symtabjava.txt and modify NdrBuffer to include float, double, hyper, ... whatever.

Should the interface class extend jarapac.rpc.Stub? That way the uuid could be exposed through it.

Improve parser validation when the parser settles down.
